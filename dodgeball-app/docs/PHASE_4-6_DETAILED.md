# 🏐 Phase 4-6 상세 구현 가이드

**작성일**: 2025-11-09
**대상 Phase**: 4 (대시보드), 5 (학생 뷰), 6 (학급/팀 관리)

---

## 📋 Phase 4: 통합 대시보드 (3-5일)

### 🎯 목표
MainApp 스타일의 세련된 대시보드 구현

### 📦 작업 목록

#### 4.1 대시보드 페이지 리뉴얼

**위치**: `app/teacher/dashboard/page.tsx`

**현재 상태**:
- 단순한 학급/경기 목록
- 카드 형태 UI
- 기본적인 네비게이션

**개선 목표**:
- MainApp 스타일 5개 뷰 탭
- 대시보드 메인 화면 4개 카드
- 빠른 액션 버튼
- 통계 위젯

**UI 구성**:
```
┌─────────────────────────────────────────────────────────────────┐
│  DodgeballHub                    👤 이원근 선생님    [설정] [⚙️]  │
├─────────────────────────────────────────────────────────────────┤
│  [📊 대시보드] [👥 학급관리] [🏐 경기] [📈 통계] [🏆 배지]     │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  대시보드 탭 화면:                                               │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌─────────┐│
│  │ 👥 학급 관리 │ │ 🏐 경기 관리 │ │ 📊 통합 통계 │ │🏆 배지 │││
│  │              │ │              │ │              │ │    도감 │││
│  │ 학급: 3개    │ │진행중: 1경기 │ │완료: 45경기  │ │21개 배지│││
│  │ 학생: 85명   │ │완료: 45경기  │ │MVP: 15명     │ │획득: 380│││
│  │              │ │              │ │              │ │        │││
│  │ [학급 관리→] │ │ [경기 시작→] │ │ [통계 보기→] │ │[보기→] │││
│  └──────────────┘ └──────────────┘ └──────────────┘ └─────────┘│
│                                                                  │
│  빠른 액션                                                       │
│  [➕ 학급 추가] [🎯 새 경기] [👥 학생 추가] [📋 보고서]         │
│                                                                  │
│  📊 학급 랭킹                                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ 1위 🥇 3학년 1반  평균 75.2점 | 학생 28명  [더보기 →]      │ │
│  │ 2위 🥈 3학년 2반  평균 72.8점 | 학생 27명  [더보기 →]      │ │
│  │ 3위 🥉 3학년 3반  평균 70.5점 | 학생 26명  [더보기 →]      │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  최근 활동                                                       │
│  • 10분 전: 3-1 김철수 - 🏆 슈퍼스타 배지 획득                  │
│  • 30분 전: 빨강팀 vs 파랑팀 경기 종료 (빨강팀 승리)            │
│  • 1시간 전: 3-2 이영희 - 💪 철인 배지 획득                     │
└─────────────────────────────────────────────────────────────────┘
```

**주요 함수**:
```typescript
const DashboardPage = () => {
  const [activeView, setActiveView] = useState<'dashboard' | 'classes' | 'games' | 'stats' | 'badges'>('dashboard');
  const [stats, setStats] = useState<DashboardStats | null>(null);

  useEffect(() => {
    loadDashboardStats();
  }, []);

  const loadDashboardStats = async () => {
    const classes = await getAllClasses();
    const students = await getAllStudents();
    const games = await getAllGames();
    const ongoingGames = games.filter(g => !g.isCompleted);
    const completedGames = games.filter(g => g.isCompleted);

    setStats({
      classCount: classes.length,
      studentCount: students.length,
      ongoingGamesCount: ongoingGames.length,
      completedGamesCount: completedGames.length,
      totalBadgesAwarded: students.reduce((sum, s) => sum + (s.badges?.length || 0), 0)
    });
  };

  return (
    <div>
      {/* 탭 네비게이션 */}
      <Tabs value={activeView} onValueChange={setActiveView}>
        <TabsList>
          <TabsTrigger value="dashboard">📊 대시보드</TabsTrigger>
          <TabsTrigger value="classes">👥 학급관리</TabsTrigger>
          <TabsTrigger value="games">🏐 경기</TabsTrigger>
          <TabsTrigger value="stats">📈 통계</TabsTrigger>
          <TabsTrigger value="badges">🏆 배지</TabsTrigger>
        </TabsList>

        <TabsContent value="dashboard">
          <DashboardMain stats={stats} />
        </TabsContent>
        {/* ... 나머지 탭 */}
      </Tabs>
    </div>
  );
};
```

---

#### 4.2 대시보드 메인 카드 컴포넌트

**파일 구조**:
```
components/dashboard/
├── DashboardCard.tsx       # 재사용 가능한 카드
├── ClassCard.tsx           # 학급 관리 카드
├── GameCard.tsx            # 경기 관리 카드
├── StatsCard.tsx           # 통계 카드
├── BadgeCard.tsx           # 배지 도감 카드
└── QuickActions.tsx        # 빠른 액션 버튼
```

**DashboardCard 예시**:
```tsx
interface DashboardCardProps {
  icon: string;
  title: string;
  stats: { label: string; value: string | number }[];
  actionLabel: string;
  onAction: () => void;
}

const DashboardCard: React.FC<DashboardCardProps> = ({
  icon,
  title,
  stats,
  actionLabel,
  onAction
}) => {
  return (
    <Card className="hover:shadow-lg transition-shadow">
      <CardHeader>
        <CardTitle className="flex items-center gap-2">
          <span className="text-2xl">{icon}</span>
          {title}
        </CardTitle>
      </CardHeader>
      <CardContent>
        {stats.map((stat, idx) => (
          <div key={idx} className="flex justify-between py-1">
            <span className="text-muted-foreground">{stat.label}</span>
            <span className="font-semibold">{stat.value}</span>
          </div>
        ))}
      </CardContent>
      <CardFooter>
        <Button onClick={onAction} className="w-full">
          {actionLabel} →
        </Button>
      </CardFooter>
    </Card>
  );
};
```

---

#### 4.3 탭 컨텐츠 컴포넌트

**학급관리 탭** (`components/dashboard/ClassesTab.tsx`):
- 학급 목록 카드
- 학생 수, 팀 수 표시
- 학급별 빠른 액션

**경기 탭** (`components/dashboard/GamesTab.tsx`):
- 진행 중 경기 / 완료된 경기 구분
- 경기 카드 (팀, 시간, 상태)
- 새 경기 시작 버튼

**통계 탭** = `StatsView.tsx` (Phase 3에서 작성)

**배지 탭** = `BadgeCollection.tsx` (이미 존재)

---

### ✅ Phase 4 체크리스트

- [ ] `app/teacher/dashboard/page.tsx` 리뉴얼
- [ ] 탭 네비게이션 구현
- [ ] `components/dashboard/DashboardCard.tsx`
- [ ] `components/dashboard/ClassCard.tsx`
- [ ] `components/dashboard/GameCard.tsx`
- [ ] `components/dashboard/StatsCard.tsx`
- [ ] `components/dashboard/BadgeCard.tsx`
- [ ] `components/dashboard/QuickActions.tsx`
- [ ] `components/dashboard/ClassesTab.tsx`
- [ ] `components/dashboard/GamesTab.tsx`
- [ ] ClassRankingWidget 통합
- [ ] 최근 활동 피드
- [ ] 테스트 및 스타일 조정

---

## 📋 Phase 5: 학생 뷰 확장 (3-5일)

### 🎯 목표
학생 대시보드 풍부화, 6자리 코드 시스템

### 📦 작업 목록

#### 5.1 학생 뷰 확장 (`StudentView.tsx`)

**위치**: `app/student/page.tsx`

**현재 상태**:
- 접근 코드 입력 ("3-5-김철수")
- 간단한 스탯 표시
- 배지 목록

**개선 목표**:
- 6자리 코드 로그인
- 대시보드 스타일 UI
- 내 통계 요약 카드
- 배지 컬렉션 갤러리
- 경기 히스토리

**UI 구성**:
```
┌─────────────────────────────────────────────────────┐
│  DodgeballHub - 학생 뷰                     [로그아웃]│
├─────────────────────────────────────────────────────┤
│  👤 김철수 (3학년 1반)                              │
├─────────────────────────────────────────────────────┤
│                                                      │
│  내 통계                                             │
│  ┌──────────────┐ ┌──────────────┐ ┌─────────────┐ │
│  │ 📊 종합 점수 │ │ 🏐 참여 경기 │ │ 🏆 배지 수  │ │
│  │     80점     │ │    15경기    │ │    12개     │ │
│  └──────────────┘ └──────────────┘ └─────────────┘ │
│                                                      │
│  ┌──────────────┐ ┌──────────────┐ ┌─────────────┐ │
│  │ 🎯 아웃      │ │ 🤝 패스      │ │ 💚 양보     │ │
│  │     25개     │ │    15개      │ │    10개     │ │
│  └──────────────┘ └──────────────┘ └─────────────┘ │
│                                                      │
│  내 배지  [배지 관리]                                │
│  ┌────────┐┌────────┐┌────────┐┌────────┐          │
│  │   🏆   ││   ⚡   ││   🌟   ││   💎   │...       │
│  │슈퍼스타││스피드왕││별의별  ││다이아  │          │
│  └────────┘└────────┘└────────┘└────────┘          │
│                                                      │
│  경기 기록                                           │
│  ┌──────────────────────────────────────────────┐  │
│  │ 2025-11-08  빨강팀 vs 파랑팀  [승리 🏆]      │  │
│  │ 아웃 5  패스 3  양보 2  🍪 4                  │  │
│  │ MVP 선정 ⭐                                   │  │
│  ├──────────────────────────────────────────────┤  │
│  │ 2025-11-07  노랑팀 vs 초록팀  [패배]         │  │
│  │ 아웃 3  패스 4  양보 1  🍪 2                  │  │
│  └──────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────┘
```

**주요 함수**:
```typescript
const StudentView = () => {
  const [student, setStudent] = useState<Student | null>(null);
  const [gameHistory, setGameHistory] = useState<GameRecord[]>([]);

  useEffect(() => {
    const studentId = localStorage.getItem('studentId');
    if (studentId) {
      loadStudent(studentId);
      loadGameHistory(studentId);
    }
  }, []);

  const loadStudent = async (id: string) => {
    const s = await getStudentById(id);
    setStudent(s);
  };

  const loadGameHistory = async (id: string) => {
    const history = await getPlayerGameHistory(id);
    setGameHistory(history);
  };

  return (
    <div>
      {/* 통계 카드 */}
      <div className="grid grid-cols-3 gap-4">
        <StatCard
          icon="📊"
          label="종합 점수"
          value={calculatePlayerPoints(student)}
        />
        <StatCard
          icon="🏐"
          label="참여 경기"
          value={student.stats.gamesPlayed}
        />
        <StatCard
          icon="🏆"
          label="배지 수"
          value={student.badges.length}
        />
      </div>

      {/* 배지 갤러리 */}
      <BadgeGallery badges={student.badges} studentId={student.id} />

      {/* 경기 히스토리 */}
      <StudentGameHistory history={gameHistory} />
    </div>
  );
};
```

---

#### 5.2 6자리 코드 시스템

**현재 방식**: "3-5-김철수" (반-번호-이름)

**새 방식**: "A3B5C7" (6자리 랜덤 코드)

**코드 생성 함수** (`lib/studentCodeGenerator.ts`):
```typescript
export function generateStudentCode(): string {
  const chars = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789';
  let code = '';

  for (let i = 0; i < 6; i++) {
    code += chars.charAt(Math.floor(Math.random() * chars.length));
  }

  return code;
}

export async function generateUniqueStudentCode(): Promise<string> {
  let code = generateStudentCode();
  let isUnique = false;

  while (!isUnique) {
    const existingStudent = await getStudentByAccessCode(code);
    if (!existingStudent) {
      isUnique = true;
    } else {
      code = generateStudentCode();
    }
  }

  return code;
}

export async function createStudentWithCode(
  classId: string,
  name: string,
  number: number
): Promise<Student> {
  const accessCode = await generateUniqueStudentCode();

  const student: Student = {
    id: generateUniqueId(),
    classId,
    name,
    number,
    classNumber: getClassNumberFromId(classId),
    accessCode,
    stats: {
      outs: 0,
      passes: 0,
      sacrifices: 0,
      cookies: 0,
      gamesPlayed: 0,
      totalScore: 0
    },
    badges: [],
    createdAt: new Date().toISOString()
  };

  await createStudent(student);
  return student;
}
```

---

#### 5.3 학생 코드 목록 모달 (`ClassStudentCodesModal.tsx`)

**위치**: `components/student/ClassStudentCodesModal.tsx`

**기능**:
- 학급별 전체 학생 코드 표시
- 복사 버튼
- 인쇄 기능

**UI 구성**:
```
┌─────────────────────────────────────────┐
│  학생 접근 코드 목록              [X]   │
├─────────────────────────────────────────┤
│  학급: 3학년 1반                        │
│  총 학생: 28명                          │
│                                         │
│  [전체 복사] [인쇄]                     │
├─────────────────────────────────────────┤
│  ┌─────────────────────────────────┐   │
│  │ 1번 김철수    A3B5C7   [복사]  │   │
│  │ 2번 이영희    X8Y2Z9   [복사]  │   │
│  │ 3번 박민수    Q4W7E1   [복사]  │   │
│  │ ...                             │   │
│  └─────────────────────────────────┘   │
│                                         │
│                [닫기]                   │
└─────────────────────────────────────────┘
```

---

#### 5.4 경기 히스토리 (`StudentGameHistory.tsx`)

**위치**: `components/student/StudentGameHistory.tsx`

**기능**:
- 학생별 경기 기록 타임라인
- 승/패 표시
- MVP 여부
- 개인 스탯

**UI 구성**:
```
┌─────────────────────────────────────────┐
│  내 경기 기록                           │
├─────────────────────────────────────────┤
│  ┌─────────────────────────────────┐   │
│  │ 2025-11-08  [승리 🏆]           │   │
│  │ 빨강팀 vs 파랑팀                │   │
│  │ 아웃 5  패스 3  양보 2  🍪 4   │   │
│  │ ⭐ MVP 선정                      │   │
│  │ [상세보기]                       │   │
│  └─────────────────────────────────┘   │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │ 2025-11-07  [패배]              │   │
│  │ 노랑팀 vs 초록팀                │   │
│  │ 아웃 3  패스 4  양보 1  🍪 2   │   │
│  │ [상세보기]                       │   │
│  └─────────────────────────────────┘   │
│                                         │
│  ...                                    │
└─────────────────────────────────────────┘
```

---

### ✅ Phase 5 체크리스트

- [ ] `app/student/page.tsx` 리뉴얼
- [ ] `lib/studentCodeGenerator.ts` 작성
- [ ] 6자리 코드 생성 로직
- [ ] 학생 생성 시 코드 자동 생성
- [ ] `components/student/ClassStudentCodesModal.tsx`
- [ ] `components/student/StudentGameHistory.tsx`
- [ ] `components/student/BadgeGallery.tsx`
- [ ] 학생 로그인 페이지 개선
- [ ] 학생 뷰 통계 카드
- [ ] 배지 관리 모달 통합
- [ ] 테스트 및 버그 수정

---

## 📋 Phase 6: 학급/팀 관리 개선 (3-5일)

### 🎯 목표
ClassTeamManagementView 스타일 통합 관리 페이지

### 📦 작업 목록

#### 6.1 통합 관리 뷰 (`ClassTeamManagementView.tsx`)

**위치**: `app/teacher/class/[classId]/manage/page.tsx` (새 페이지)

**기능**:
- 학급 정보 + 학생 목록 + 팀 목록을 한 화면에
- 드래그앤드롭으로 학생 ↔ 팀 이동
- 학생 카드에 배지 표시
- 학생 통계 자동 계산

**UI 구성**:
```
┌─────────────────────────────────────────────────────────────────┐
│  3학년 1반 관리                                  [학급 설정] [⚙️] │
├─────────────────────────────────────────────────────────────────┤
│  [👥 학생] [⚽ 팀] [📊 통계]                                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  학생 탭:                                                        │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ 총 28명  [➕ 학생 추가] [📋 코드 목록] [🔀 랜덤 팀 편성]  │ │
│  ├────────────────────────────────────────────────────────────┤ │
│  │ ┌──────────────────────────────────────────────────────┐  │ │
│  │ │ ⠿ 1번 김철수 (3-1)  [🏆][⚡][🌟]  A3B5C7         │  │ │
│  │ │ 아웃 25  패스 15  양보 10  🍪 30  총점 80         │  │ │
│  │ │ 15경기  [수정] [삭제]                             │  │ │
│  │ └──────────────────────────────────────────────────────┘  │ │
│  │ ┌──────────────────────────────────────────────────────┐  │ │
│  │ │ ⠿ 2번 이영희 (3-1)  [🎯][💪][🤝]  X8Y2Z9         │  │ │
│  │ │ 아웃 20  패스 18  양보 12  🍪 25  총점 75         │  │ │
│  │ │ 12경기  [수정] [삭제]                             │  │ │
│  │ └──────────────────────────────────────────────────────┘  │ │
│  │ ...                                                        │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  팀 탭:                                                          │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ [빨강팀] [파랑팀] [노랑팀]  [➕ 팀 추가]                   │ │
│  │                                                              │ │
│  │ 빨강팀 (7명)                                                │ │
│  │ ┌──────────────────────────────────────────────────────┐  │ │
│  │ │ ⠿ 김철수  [🏆][⚡][🌟]  아웃 25  총점 80          │  │ │
│  │ │ ⠿ 박민수  [🎯][💪]  아웃 18  총점 74              │  │ │
│  │ │ ...                                                    │  │ │
│  │ └──────────────────────────────────────────────────────┘  │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

**주요 함수**:
```typescript
const ClassTeamManagementView = ({ classId }: { classId: string }) => {
  const [tab, setTab] = useState<'students' | 'teams' | 'stats'>('students');
  const [students, setStudents] = useState<Student[]>([]);
  const [teams, setTeams] = useState<Team[]>([]);

  useEffect(() => {
    loadClassData();
  }, [classId]);

  const loadClassData = async () => {
    const cls = await getClassById(classId);
    const studs = await getStudentsByClassId(classId);
    const tms = await getTeamsByClassId(classId);

    setStudents(studs);
    setTeams(tms);
  };

  return (
    <div>
      <Tabs value={tab} onValueChange={setTab}>
        <TabsList>
          <TabsTrigger value="students">👥 학생</TabsTrigger>
          <TabsTrigger value="teams">⚽ 팀</TabsTrigger>
          <TabsTrigger value="stats">📊 통계</TabsTrigger>
        </TabsList>

        <TabsContent value="students">
          <StudentsList students={students} onUpdate={loadClassData} />
        </TabsContent>

        <TabsContent value="teams">
          <TeamsView teams={teams} students={students} onUpdate={loadClassData} />
        </TabsContent>

        <TabsContent value="stats">
          <ClassStats classId={classId} students={students} />
        </TabsContent>
      </Tabs>
    </div>
  );
};
```

---

#### 6.2 학생 카드 UI 개선 (`EnhancedStudentCard.tsx`)

**위치**: `components/teacher/EnhancedStudentCard.tsx`

**기능**:
- 배지 표시 (PlayerBadgeDisplay 사용)
- 드래그 가능
- 통계 요약
- 빠른 액션 버튼

**UI 구성**:
```
┌──────────────────────────────────────────────────────┐
│ ⠿ 1번 김철수 (3-1)                                   │
│                                                       │
│ [🏆][⚡][🌟][+9]  A3B5C7                            │
│                                                       │
│ 📊 아웃 25  🤝 패스 15  💚 양보 10  🍪 쿠키 30      │
│ 🏐 15경기  ⭐ 종합 80점                              │
│                                                       │
│ [📝 수정] [🏆 배지] [📋 기록] [🗑️ 삭제]              │
└──────────────────────────────────────────────────────┘
```

---

#### 6.3 드래그앤드롭 개선

**현재 상태**:
- 기본적인 드래그앤드롭 (@dnd-kit)
- 팀 편성 페이지에서만 사용

**개선 목표**:
- Sortable 로직 추가 (순서 변경)
- 부드러운 애니메이션
- 시각적 피드백 향상

**주요 변경**:
```typescript
import { DndContext, closestCenter } from '@dnd-kit/core';
import { SortableContext, verticalListSortingStrategy, useSortable } from '@dnd-kit/sortable';
import { CSS } from '@dnd-kit/utilities';

const SortableStudentCard = ({ student }: { student: Student }) => {
  const {
    attributes,
    listeners,
    setNodeRef,
    transform,
    transition,
  } = useSortable({ id: student.id });

  const style = {
    transform: CSS.Transform.toString(transform),
    transition,
  };

  return (
    <div ref={setNodeRef} style={style} {...attributes} {...listeners}>
      <EnhancedStudentCard student={student} />
    </div>
  );
};
```

---

#### 6.4 학생 통계 자동 계산

**위치**: `lib/statsCalculator.ts` (확장)

**함수 추가**:
```typescript
// 학생 생성 시 초기 통계
export function createInitialStudentStats(): StudentStats {
  return {
    outs: 0,
    passes: 0,
    sacrifices: 0,
    cookies: 0,
    gamesPlayed: 0,
    totalScore: 0
  };
}

// 경기 기록을 학생 통계에 누적
export function addGameRecordToStats(
  currentStats: StudentStats,
  gameRecord: GameRecord
): StudentStats {
  return {
    outs: currentStats.outs + gameRecord.outs,
    passes: currentStats.passes + gameRecord.passes,
    sacrifices: currentStats.sacrifices + gameRecord.sacrifices,
    cookies: currentStats.cookies + gameRecord.cookies,
    gamesPlayed: currentStats.gamesPlayed + 1,
    totalScore: calculatePlayerPoints({
      ...currentStats,
      outs: currentStats.outs + gameRecord.outs,
      passes: currentStats.passes + gameRecord.passes,
      sacrifices: currentStats.sacrifices + gameRecord.sacrifices,
      cookies: currentStats.cookies + gameRecord.cookies,
    } as Student)
  };
}

// 음수 방지
export function normalizeStats(stats: StudentStats): StudentStats {
  return {
    outs: Math.max(0, stats.outs),
    passes: Math.max(0, stats.passes),
    sacrifices: Math.max(0, stats.sacrifices),
    cookies: Math.max(0, stats.cookies),
    gamesPlayed: Math.max(0, stats.gamesPlayed),
    totalScore: Math.max(0, stats.totalScore)
  };
}
```

---

### ✅ Phase 6 체크리스트

- [ ] `app/teacher/class/[classId]/manage/page.tsx` 작성
- [ ] `components/teacher/EnhancedStudentCard.tsx`
- [ ] `components/teacher/StudentsList.tsx`
- [ ] `components/teacher/TeamsView.tsx`
- [ ] `components/teacher/ClassStats.tsx`
- [ ] PlayerBadgeDisplay 통합
- [ ] Sortable 드래그앤드롭 개선
- [ ] 학생 통계 자동 계산 로직
- [ ] 빠른 액션 버튼 (수정, 배지, 기록, 삭제)
- [ ] 테스트 및 버그 수정

---

## 📝 다음 단계

Phase 4-6 완료 후:
1. `UI_COMPONENT_GUIDE.md` - 컴포넌트별 UI 상세 가이드
2. `FIREBASE_AUTOMATION.md` - Firebase 자동 마이그레이션 가이드

**마지막 업데이트**: 2025-11-09
